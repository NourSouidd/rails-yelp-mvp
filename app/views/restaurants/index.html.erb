<div class="container p-3">
  <h1>Restaurants</h1>

  <ul>

    <% @restaurants.each do |r| %>
     <li><%= link_to r.name, restaurant_path(r) %></li>
     <p><%= link_to "Delete", restaurants_path(@restaurant), method: :delete %></p>
    <% end %>
  </ul>
<br>

<%= link_to 'Add a new Restaurant', new_restaurant_path %>

</div>
